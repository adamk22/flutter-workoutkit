enum WorkoutAlertType {
  cadenceRange,
  cadenceThreshold,
  heartRateRange,
  heartRateZone,
  powerRange,
  powerThreshold,
  powerZone,
  speedRange,
  speedThreshold,
  paceRange,
  paceThreshold,
}
